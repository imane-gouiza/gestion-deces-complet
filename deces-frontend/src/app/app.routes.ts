import { Routes } from '@angular/router';
import { LoginComponent } from './login/login'; // Hna khass tkoun smit l-fichier .ts bla .ts

export const routes: Routes = [
    { path: '', component: LoginComponent }
];